import 'package:flutter_test/flutter_test.dart';

void main() {
  group("Test the firebase login data source", () {
    test(
        "check current user",
        () => {
              //Act

              //Arrange

              //Assert
            });

    test(
        "check Google api call",
        () => {
              //Act

              //Arrange

              //Assert
            });

    test(
        "check if user signed in",
        () => {
              //Act

              //Arrange

              //Assert
            });

    test(
        "check if user sign out",
        () => {
              //Act

              //Arrange

              //Assert
            });
  });
}
